
-- glossaire
  i <Entier>,
  message <Chaine>,
  message_encrypté <Chaine>,
  clef <Chaine>;

début
-- Encrypter un message
  -- On demande le message
    lire(message);
  -- On demande la clef
    lire(clef);

  -- On change la longueur de la clef afin
  -- qu'elle soit identique à celle du message
  -- on pose l'hypothèse qu'il existe un sous-programme 'longueur' tel que:
  -- module longueur
  -- renvoie <Entier>
  -- paramètres:
  --  entrée:
  --    string <Chaine>; - chaine dont on veut obtenir la longueur
  -- description: renvoie la longueur d'une chaine donnée
    tantque i /= longueur(message) faire

      -- On utilise i mod longueur(clef) afin que 
      --l'indexe réferencé ne dépasse pas la longueur de la clef
      -- +1 car l'indexe 0 du tableau n'est pas utilisé
      message_encrypté <- message_encrypté + crypterCaractère(message[i]+1), valeur(clef[i mod longueur(clef) +1]) );
      i <- i+1;
    fin tantque;

    afficher(message_encrypté);
fin
--
--
--
--
--
--
--
--
--
--
--
--
--
--
--
--
--
--
